setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    web: web/Dockerfile
    worker: worker/Dockerfile
release:
  image: worker
run:
  web: node worker/dist/src/index.js
